const {chromium}=require('@playwright/test');const fs=require('node:fs');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:process.env.CHROME_PATH});try{const page=await browser.newPage();await page.goto('http://127.0.0.1:3000');const data=await page.evaluate(async()=>{const v=document.createElement('video');v.muted=true;v.src='/video/hero-cinematic.mp4';await new Promise((resolve,reject)=>{v.onloadeddata=resolve;v.onerror=reject;});v.currentTime=.1;await new Promise(resolve=>v.onseeked=resolve);const c=document.createElement('canvas');c.width=960;c.height=Math.round(960*v.videoHeight/v.videoWidth);c.getContext('2d').drawImage(v,0,0,c.width,c.height);return c.toDataURL('image/webp',.8).split(',')[1];});fs.writeFileSync('public/video/hero-poster.webp',Buffer.from(data,'base64'));}finally{await browser.close();}})().catch(e=>{console.error(e);process.exit(1)});
